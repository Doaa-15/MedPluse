name: reminder
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  equatable: ^2.0.8
  hive_flutter: ^1.1.0
  dartz: ^0.10.1
  get_it: ^9.2.1
  flutter_bloc: ^9.1.1
  intl: ^0.19.0      # عدلت هذا ليتوافق مع Dart 3.4
  uuid: ^4.5.3
  supabase_flutter: ^2.0.0
  image_picker: ^1.1.0 # نسخة مستقرة لـ SDK القديم
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.0
  flutter_timezone: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # نسخة متوافقة
  build_runner: ^2.4.8
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true